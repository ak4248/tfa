#!/bin/nash
# this means bash will the program
# created by ak4248
sum=0
# OK is this a comment?
# to pass all variables - add @
# if fixed variable then $V $X with space
# test to add a comment
for arg in $@
do
    sum=$((arg+sum))
done
echo $sum

